import { Typography } from '@mui/material';
import React from 'react';

/**
 * Pages is rendered during unauthorized access
 */
const Unauthorized = () => {
	return <Typography variant={'h1'}>Unauthorized</Typography>;
};

export default Unauthorized;
